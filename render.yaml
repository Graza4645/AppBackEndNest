services:
  - type: web
    name: teamlease-backend
    env: node
    rootDir: .
    buildCommand: npm install && npx tsc -p tsconfig.build.json
    startCommand: node dist/src/main.js
    envVars:
      - key: NODE_ENV
        value: production